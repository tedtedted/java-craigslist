org.gradle.jvmargs=-Xmx2g -XX:+UseG1GC
org.gradle.parallel=true
org.gradle.caching=true
# Reuse the result of Gradle's configuration phase across invocations.
# Drop this line (or set to false) if a plugin you add later turns out to be incompatible.
org.gradle.configuration-cache=true
